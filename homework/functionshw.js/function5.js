
function checkLength(str1) {
    
    return str1.length; // we use length metod to get string length
}


let myString1 = "Hello, World!";
let length = checkLength(myString1);
console.log(`the length of mystring:  ${length} `);

// Output: the length of mystring:  13